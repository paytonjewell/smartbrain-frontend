import React from 'react';

function Logo(props) {
    return (
        <div className={'ma4 mt0 grow pointer'} style={{fontSize: 150, textShadow: '0 0 10px rgba(0, 0, 0, 0.5)'}}>
            🧠
        </div>
    );
}

export default Logo;